import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Lemuel Oruma | Backend Developer & System Architect",
  description: "Portfolio of Lemuel Oruma, a software engineer specializing in scalable APIs, database optimization, and reliable system infrastructure.",
  keywords: ["Backend Developer", "Node.js", "TypeScript", "System Architect", "APIs", "Database Optimization"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
